import { LocalStorageService } from 'angular-2-local-storage';
import { HttpClient } from '@angular/common/http';
import { Router } from '@angular/router';
import { EventEmitter } from '@angular/core';
export declare class AuthService {
    private router;
    private httpClient;
    private localStorageService;
    codeDomain: string;
    searchingCode: EventEmitter<boolean>;
    codeNotFound: EventEmitter<boolean>;
    private accessToken;
    constructor(router: Router, httpClient: HttpClient, localStorageService: LocalStorageService);
    isAuthenticated(): boolean;
    login(email: string, password: string): void;
    logout(): void;
    getAccessToken(): string;
    setCode(domain: string): void;
    getCode(): string;
    private parseAccessToken;
}
