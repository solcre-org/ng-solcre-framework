import { OnInit } from '@angular/core';
import { FormGroup, FormBuilder } from '@angular/forms';
import { AuthService } from './solcre-auth-library.service';
export declare class SolcreAuthLibraryComponent implements OnInit {
    private formBuilder;
    private authService;
    signinForm: FormGroup;
    searchingCode: boolean;
    codeNotFound: boolean;
    constructor(formBuilder: FormBuilder, authService: AuthService);
    ngOnInit(): void;
    onSubmit(): void;
    onBlur(): void;
}
